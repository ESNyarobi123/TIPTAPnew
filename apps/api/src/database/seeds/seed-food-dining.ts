/**
 * Vertical-specific extensions for FOOD_DINING seed data.
 * Core demo tenant data is created in `seed.ts`.
 */
export async function seedFoodDiningExtensions(): Promise<void> {
  // Phase 4: extra branches, menus, or QR fixtures.
}
