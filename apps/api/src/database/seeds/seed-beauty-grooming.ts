/**
 * Vertical-specific extensions for BEAUTY_GROOMING seed data.
 * Core demo tenant data is created in `seed.ts`.
 */
export async function seedBeautyGroomingExtensions(): Promise<void> {
  // Phase 5: extra stations, services, or QR fixtures.
}
